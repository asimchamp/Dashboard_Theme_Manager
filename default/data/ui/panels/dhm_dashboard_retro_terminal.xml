<panel depends="$alwaysHideCSS$">
   <html>
      <style>
         /* ======================================================================
         RETRO TERMINAL THEME - 1980s CRT MONITOR AESTHETIC
         Classic Terminal with Neon Green Text on Black
         HTML AND CSS ONLY - NO SCRIPT
         ====================================================================== */
         
         /* ----------------------------------------------------------------------
         DESIGN TOKENS - TERMINAL CRT
         ---------------------------------------------------------------------- */
         :root {
            --color-bg-base: #000000;
            --color-bg-elevated: #0a0a0a;
            --color-bg-hover: #001100;
            --color-text-primary: #00ff00;
            --color-text-secondary: #00cc00;
            --color-text-muted: #008800;
            --color-accent-glow: rgba(0, 255, 0, 0.6);
            --color-border: #00ff00;
            --radius-none: 0px;
            --radius-xs: 1px;
            --space-1: 4px;
            --space-2: 8px;
            --space-3: 12px;
            --space-4: 16px;
            --space-5: 20px;
            --space-6: 24px;
         }
         
         /* ----------------------------------------------------------------------
         GLOBAL - CRT SCANLINE EFFECT
         ---------------------------------------------------------------------- */
         body {
            background: var(--color-bg-base);
            color: var(--color-text-primary);
            font-family: "Courier New", Courier, monospace;
            font-weight: 400;
         }
         
         .dashboard-body {
            background: linear-gradient(to bottom,
               transparent 0%,
               rgba(0, 255, 0, 0.02) 50%,
               transparent 100%);
            background-size: 100% 2px;
            animation: scanline 8s linear infinite;
         }
         
         @keyframes scanline {
            0% { background-position: 0 0; }
            100% { background-position: 0 100%; }
         }
         
         /* ----------------------------------------------------------------------
         HEADER - TERMINAL PROMPT
         ---------------------------------------------------------------------- */
         .dashboard-header {
            padding: var(--space-6) var(--space-5);
            border: 2px solid var(--color-border);
            border-radius: var(--radius-none);
            background: var(--color-bg-elevated);
            animation: terminalBoot 0.3s steps(10) both;
         }
         
         @keyframes terminalBoot {
            0% { opacity: 0; }
            100% { opacity: 1; }
         }
         
         .dashboard-title {
            font-size: 20px;
            font-weight: 400;
            text-transform: uppercase;
            letter-spacing: 0.1em;
            color: var(--color-text-primary);
            text-shadow: 0 0 8px var(--color-accent-glow);
         }
         
         .dashboard-title::before {
            content: "root@splunk:~# ";
            color: var(--color-text-secondary);
         }
         
         .dashboard-description {
            font-size: 12px;
            color: var(--color-text-muted);
            margin-top: var(--space-2);
            font-family: "Courier New", Courier, monospace;
         }
         
         /* ----------------------------------------------------------------------
         THEME BADGE
         ---------------------------------------------------------------------- */
         .dashboard-header::after {
            content: "RETRO TERMINAL";
            position: fixed;
            top: 125px;
            right: var(--space-6);
            padding: var(--space-2) var(--space-4);
            font-size: 10px;
            letter-spacing: 0.2em;
            background: var(--color-bg-base);
            color: var(--color-text-primary);
            border: 1px solid var(--color-border);
            box-shadow: 0 0 10px var(--color-accent-glow);
            font-family: "Courier New", Courier, monospace;
            pointer-events: none;
            z-index: 999;
         }
         
         /* ----------------------------------------------------------------------
         WATERMARK
         ---------------------------------------------------------------------- */
         .dashboard-body::before {
            content: "[SYSTEM READY]";
            position: fixed;
            bottom: 18px;
            right: 22px;
            font-size: 10px;
            color: var(--color-text-muted);
            font-family: "Courier New", Courier, monospace;
            pointer-events: none;
            z-index: 0;
         }
         
         /* ----------------------------------------------------------------------
         INPUTS
         ---------------------------------------------------------------------- */
         .input {
            background: var(--color-bg-elevated);
            border: 1px solid var(--color-border);
            padding: var(--space-3);
            margin-bottom: var(--space-4);
         }
         
         .input label {
            font-size: 11px;
            color: var(--color-text-primary);
            text-transform: uppercase;
            letter-spacing: 0.1em;
            font-family: "Courier New", Courier, monospace;
         }
         
         .input label::before {
            content: " \003c";
         }

         .input label::after {
            content: " \003e";
         }
         
         .input select,
         .input input {
            width: 100%;
            background: var(--color-bg-base);
            color: var(--color-text-primary);
            border: 1px solid var(--color-text-muted);
            padding: var(--space-2);
            font-family: "Courier New", Courier, monospace;
         }
         
         .input select:focus,
         .input input:focus {
            outline: none;
            box-shadow: 0 0 8px var(--color-accent-glow);
         }
         
         /* ----------------------------------------------------------------------
         PANELS
         ---------------------------------------------------------------------- */
         .dashboard-panel {
            background: var(--color-bg-elevated);
            border: 2px solid var(--color-border);
            padding: var(--space-5);
            margin-bottom: var(--space-5);
            box-shadow: 0 0 15px rgba(0, 255, 0, 0.2);
            animation: terminalPanelBoot 0.4s steps(5) both;
         }
         
         @keyframes terminalPanelBoot {
            0% { opacity: 0; transform: scaleY(0); }
            100% { opacity: 1; transform: scaleY(1); }
         }
         
         .dashboard-panel:hover {
            box-shadow: 0 0 25px rgba(0, 255, 0, 0.4);
         }
         
         .dashboard-panel .panel-head {
            border-bottom: 1px solid var(--color-border);
            padding-bottom: var(--space-2);
            margin-bottom: var(--space-3);
         }
         
         .dashboard-panel .panel-title {
            font-size: 12px;
            color: var(--color-text-primary);
            text-transform: uppercase;
            letter-spacing: 0.1em;
            font-family: "Courier New", Courier, monospace;
         }
         
         .dashboard-panel .panel-title::before {
            content: "[";
            margin-right: 4px;
         }
         
         .dashboard-panel .panel-title::after {
            content: "]";
            margin-left: 4px;
         }
         
         /* ----------------------------------------------------------------------
         KPI
         ---------------------------------------------------------------------- */
         .single-value {
            font-size: 48px;
            font-weight: 700;
            color: var(--color-text-primary);
            font-family: "Courier New", Courier, monospace;
            text-shadow: 0 0 12px var(--color-accent-glow);
         }
         
         .single-value-title {
            font-size: 11px;
            color: var(--color-text-secondary);
            text-transform: uppercase;
            letter-spacing: 0.12em;
            margin-top: var(--space-2);
            font-family: "Courier New", Courier, monospace;
         }
         
         .single-value::after {
            content: "_";
            animation: blink 1s step-end infinite;
         }
         
         @keyframes blink {
            0%, 50% { opacity: 1; }
            51%, 100% { opacity: 0; }
         }
         
         /* ----------------------------------------------------------------------
         CHARTS
         ---------------------------------------------------------------------- */
         .chart svg {
            filter: drop-shadow(0 0 8px rgba(0, 255, 0, 0.3));
         }
         
         .highcharts-series rect {
            rx: 0;
            ry: 0;
         }
         
         .highcharts-graph {
            stroke-width: 2;
         }
         
         .highcharts-axis-labels text,
         .highcharts-legend-item text {
            fill: var(--color-text-primary);
            font-family: "Courier New", Courier, monospace;
            font-size: 10px;
         }
         
         .highcharts-axis-line,
         .highcharts-grid-line {
            stroke: var(--color-text-muted);
         }
         
         /* ----------------------------------------------------------------------
         TABLES
         ---------------------------------------------------------------------- */
         table {
            width: 100%;
            border-collapse: collapse;
            font-family: "Courier New", Courier, monospace;
         }
         
         thead th {
            background: var(--color-bg-base);
            color: var(--color-text-primary);
            padding: var(--space-2);
            border: 1px solid var(--color-border);
            text-transform: uppercase;
            font-size: 10px;
            letter-spacing: 0.1em;
         }
         
         tbody tr {
            background: var(--color-bg-elevated);
         }
         
         tbody tr:hover {
            background: var(--color-bg-hover);
            box-shadow: 0 0 10px rgba(0, 255, 0, 0.2);
         }
         
         tbody td {
            padding: var(--space-2);
            border: 1px solid var(--color-text-muted);
            color: var(--color-text-secondary);
            font-size: 11px;
         }
         
         /* ----------------------------------------------------------------------
         TOOLTIPS
         ---------------------------------------------------------------------- */
         .highcharts-tooltip {
            filter: drop-shadow(0 0 8px rgba(0, 255, 0, 0.4));
         }
         
         .highcharts-tooltip text {
            fill: var(--color-text-primary) !important;
            font-family: "Courier New", Courier, monospace !important;
         }
         
         .highcharts-tooltip-box {
            fill: var(--color-bg-base);
            stroke: var(--color-border);
            stroke-width: 2;
         }
         
         /* ----------------------------------------------------------------------
         SCROLLBAR
         ---------------------------------------------------------------------- */
         ::-webkit-scrollbar {
            width: 12px;
         }
         
         ::-webkit-scrollbar-track {
            background: var(--color-bg-base);
         }
         
         ::-webkit-scrollbar-thumb {
            background: var(--color-text-primary);
            box-shadow: 0 0 8px var(--color-accent-glow);
         }
         
         /* ----------------------------------------------------------------------
         BUTTONS
         ---------------------------------------------------------------------- */
         .btn, button {
            background: var(--color-bg-base);
            color: var(--color-text-primary);
            border: 1px solid var(--color-border);
            padding: var(--space-2) var(--space-4);
            font-family: "Courier New", Courier, monospace;
            text-transform: uppercase;
            cursor: pointer;
         }
         
         .btn:hover, button:hover {
            box-shadow: 0 0 10px var(--color-accent-glow);
         }
         
         /* ----------------------------------------------------------------------
         TERMINAL CURSOR ANIMATION
         ---------------------------------------------------------------------- */
         .dashboard-panel:nth-child(1) { animation-delay: 0.1s; }
         .dashboard-panel:nth-child(2) { animation-delay: 0.2s; }
         .dashboard-panel:nth-child(3) { animation-delay: 0.3s; }
         .dashboard-panel:nth-child(4) { animation-delay: 0.4s; }
         
         /* ----------------------------------------------------------------------
         END RETRO TERMINAL THEME
         ---------------------------------------------------------------------- */
      </style>
   </html>
</panel>
